[project]
name = "my-simple-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
myapp = "my_simple_app.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
package = true